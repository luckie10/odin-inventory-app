extends layout

block content
  h1= title
  if category_list.length 
    ul
      each cat in category_list 
        li
          a(href=cat.url) #{cat.name} 
  else
    p There are no categories.
  
